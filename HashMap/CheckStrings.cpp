#include<bits/stdc++.h> 
using namespace std;

class Solution{
    public:
    bool  check(vector<string> s,vector<string> t){
        unordered_map<string, int> mp;
        int n = s.size(), m = t.size();
        
    }
};

int main(){
    Solution ss;
    vector<string> s = {"abc", "d"};
    vector<string> t = {"ab", "cd"};
    cout<<ss.check(s, t);
    return 0;
}